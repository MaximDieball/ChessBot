uses libv3 -> still allowing a few rare illegal moves
libv3 is libv2 converted to c++ for better performance

calculates all moves possible
selects move with best eval
eval adds up the piece values and adds opening, mid game or end game value tables
uses alpha beta pruning and reverts moves on one board obj instead of cloning the board obj for better performance
can check most of the mate

